//Josephus环问题（教材72页题目一）
//设编号为1, 2, ..., n的n个人按顺时针方向围坐一圈，约定编号为k（1≤k≤n）的人按顺时针方向从1开始报数，数到m的那个人出列，它的下一位又从1开始报数，
//数到m的那个人又出列，依次类推，直到所有人出列为止，由此产生一个出队编号的序列。试设计算法求出n个人的出列顺序。
//
//要求用户指定人数n，第一个开始报数的人的编号k及报数上限值m。然后，按照出列的顺序打印出相应的编号序列。
//
//选做内容：1）在顺序结构上实现本算法。需要考虑如何实现循环的顺序结构。
//2）m不再固定。假设n个人每人持有一个密码（正整数），从编号为k的人开始从1开始顺序报数，报到m的人出列，此时将他的密码作为新的m值，
//从他顺时针方向上的下一个人开始重新从1报数，报到m的人出列，而后将他的密码作为新的m值，如此循环下去，直到所有人全部出列为止。
//3）显示仿真的运行界面。
#include <stdio.h>
#include <stdlib.h>

// 定义人员结构体
typedef struct Person {
    int number;     // 编号
    int password;   // 密码（用于选做内容2）
    struct Person *next;  // 指向下一个人的指针
} Person;
